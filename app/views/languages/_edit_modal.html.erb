<%= render 'shared/modal_header', id: "edit_cv_language_#{@language.id}",
title: t('content.main.cv.edit.languages.title') do
 %><%= render 'form', id: "edit_cv_language_#{@language.id}" %>
<% end %>
