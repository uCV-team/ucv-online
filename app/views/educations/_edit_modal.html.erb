<% render 'shared/modal_header', id: "edit_cv_education_#{@education.id}",
title: t('content.main.cv.edit.education.title') do
 %><%= render 'form', id: "edit_cv_education_#{@education.id}" %>
<% end %>
